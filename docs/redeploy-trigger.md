# Redeploy Trigger

This file exists to trigger a new Vercel deployment so the latest OpenAI vision fix (image_url format in /api/brand/style) is picked up in production.

Commit time: ${new Date().toISOString()}